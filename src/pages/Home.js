import { Typography } from "@mui/material";
import React from "react";

const Home = () => {
  return (
    <>
      <Typography variant="h1" color="error">{`Welcome :)`}</Typography>
    </>
  );
};

export default Home;
